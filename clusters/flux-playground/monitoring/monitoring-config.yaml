apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring-config
  namespace: flux-system
spec:
  interval: 10m
  prune: true
  wait: true
  dependsOn:
    - name: monitoring-apps
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./clusters/flux-playground/monitoring/config
