apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dev-gateway-tls
  namespace: dev
spec:
  secretName: dev-gateway-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - whoami.dev.localhost
  commonName: whoami.dev.localhost
