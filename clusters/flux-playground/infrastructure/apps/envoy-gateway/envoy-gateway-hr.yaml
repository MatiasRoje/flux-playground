apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: infra
spec:
  interval: 10m
  releaseName: eg
  chartRef:
    kind: OCIRepository
    name: envoy-gateway
    namespace: flux-system
  dependsOn:
    - name: kyverno
      namespace: infra
  install:
    crds: Skip
  upgrade:
    crds: Skip
